pipeline {
    agent any
    options {
      timeout(time: 1, unit: 'SECONDS')
      }
    stages {
        stage('Test') {
            steps {
                echo 'Hello master pipeline'
              }
          }
      }
      name 'micro_flask'
  }
