pipeline {
    agent any
    options {
      timeout(time: 1, unit: 'SECONDS')
      }
    stages {
        stage('Test') {
            steps {
                echo 'Hello production pipeline'
              }
          }
      }
      name 'micro_flask'
  }
